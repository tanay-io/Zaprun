import { AuthResolver } from "../types";

// Structure-only scaffold. Implementation is intentionally deferred.
export const resolveBasicAuth: AuthResolver = async () => {
  throw new Error("NOT_IMPLEMENTED:resolveBasicAuth");
};
